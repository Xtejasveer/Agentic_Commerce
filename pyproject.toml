[project]
name = "agentic-commerce"
version = "0.1.0"
description = "AI-native merchant infrastructure — MCP gateway for AI buyers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # Existing
    "chromadb>=0.5.0",
    "fastapi[standard]==0.115.0",
    "psycopg2-binary>=2.9.9",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "razorpay>=1.4.1",
    "sqlalchemy>=2.0.0",
    # New — MCP Server
    "fastmcp>=2.0.0",
    # New — LangChain + LangGraph agent
    "langchain>=0.3.0",
    "langchain-openai>=0.3.0",
    "langgraph>=0.2.0",
    "langchain-mcp-adapters>=0.1.0",
    # New — Utilities
    "httpx>=0.27.0",
    "rich>=13.0.0",
]